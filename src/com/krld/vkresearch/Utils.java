package com.krld.vkresearch;

import com.krld.vkresearch.model.Point;

public class Utils
{


	public static double getDistance(Point p0)
	{
		return Math.sqrt(Math.pow(0 - p0.x,2) + Math.pow(0 - p0.y, 2));
	}}
